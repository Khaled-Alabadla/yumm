# Core
django
mysqlclient

# Image processing (required for ImageField in Restaurant and MenuItem)
Pillow

# API
djangorestframework
django-cors-headers

# Environment & deployment
python-dotenv
gunicorn
